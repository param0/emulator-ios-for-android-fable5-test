<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose supplies the real theming; this base only needs to be a
         Material3 no-action-bar parent so the Activity hosts Compose cleanly. -->
    <style name="Theme.IOSEmulator" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
